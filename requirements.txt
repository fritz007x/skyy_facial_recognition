# Skyy Facial Recognition MCP Server Dependencies
# Versions based on working development environment

# MCP Server Framework
fastmcp==2.13.0.2
mcp==1.20.0

# Facial Recognition
# IMPORTANT FOR WINDOWS: InsightFace 0.7.3 has NO binary on PyPI
# You MUST use the included wheel file for Windows without C++ compiler:
#   pip install insightface-0.7.3-cp311-cp311-win_amd64.whl
#
# For Linux/macOS or Windows with C++ build tools:
#   insightface==0.7.3
# (Uncomment the line below only if you have C++ build tools installed)
# insightface==0.7.3

# Deep Learning - MUST BE FIRST
# CRITICAL: Install NumPy BEFORE opencv-python to prevent version conflicts
# InsightFace 0.7.3 wheel requires NumPy 1.x
numpy==1.26.4
onnx==1.19.1
onnxruntime==1.23.2

# Image Processing
opencv-python==4.12.0.88
opencv-python-headless==4.12.0.88
pillow==12.0.0

# InsightFace Dependencies
# Required by InsightFace but not installed with --no-deps
tqdm==4.67.1
matplotlib==3.10.7
scipy==1.16.3
scikit-learn==1.7.2
scikit-image==0.25.2
Cython==3.1.6
easydict==1.13
prettytable==3.16.0
albumentations==2.0.8

# Data Validation
pydantic==2.12.3
pydantic-settings==2.11.0

# Vector Database
chromadb==1.3.5

# OAuth 2.1 and Security
PyJWT==2.10.1
cryptography==46.0.3
python-multipart==0.0.20

# Audit Logging
loguru==0.7.3

# Voice Assistant - Vosk-based (offline, lightweight, grammar support)
ollama==0.6.1
sounddevice==0.5.3
soundfile==0.13.1
pyttsx3>=2.90  # Keep version range as requested
vosk==0.3.45  # Lightweight offline speech recognition with grammar support
